Memory-game:

Desktop-window:
- X should work for closing window

Chat-app:
- Username should be able to set username, and be set to local storage
- Should always be scrolled to the bottom
- Should not scroll outside the width when full